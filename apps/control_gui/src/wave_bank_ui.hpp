#pragma once

struct App;

/** Full-page wave bank: 8 slots, folder load, background upload, play. */
void DrawWaveBankPage(App &app);

/** Compact Notes/Wave mode switch for Tone tab. */
void DrawPlayModeStrip(App &app);
