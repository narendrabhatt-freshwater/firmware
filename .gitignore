.DS_Store
**/.DS_Store

# Python bytecode from tools/ and scripts/
**/__pycache__/
*.py[cod]

# Build output and tool caches (per-app .gitignore files also cover these)
**/build/
.cache/
**/.cache/
compile_commands.json

# Dear ImGui window-layout state (cmi_control writes it to the launch cwd)
imgui.ini

# Local photo dumps, not part of the product tree
docs/img/

# Local agent state and unversioned assets
.serena/
public/
