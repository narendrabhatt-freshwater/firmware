# Auto-load Freshwater env when entering this directory (requires direnv).
# Install: brew install direnv
# Hook:    eval "$(direnv hook zsh)"   # add to ~/.zshrc once
# Allow:   direnv allow
source_env scripts/env.sh
